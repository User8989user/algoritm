"""
Передвинуть чётные числа вперёд
Дан неотсортированный массив целых чисел.
Необходимо перенести в начало массива все чётные числа, сохраняя их относительный порядок.
Пример:
Ввод: [3, 2, 4, 1, 11, 8, 9]
Вывод: 2, 4, 8, 1, 11, 3, 9
Что нужно сделать:
Реализуйте стабильное перераспределение элементов, сохраняя порядок чётных чисел. 
Объясните, за счёт чего сохраняется относительный порядок элементов.
"""

def move_even_to_front_stable(arr):
    evens = [x for x in arr if x % 2 == 0]
    odds = [x for x in arr if x % 2 != 0]
    # Изменяем исходный массив, если нужно
    arr[:] = evens + odds

# Пример
a = [3, 2, 4, 1, 11, 8, 9]
move_even_to_front_stable(a)
print(a)  # [2, 4, 8, 1, 11, 3, 9]

"""
При формировании списков evens и odds мы проходим по исходному массиву слева направо
 и добавляем элементы в том же порядке. 
 Конкатенация даёт желаемый результат.

"""